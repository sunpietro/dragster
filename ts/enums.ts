export enum EDomEvent {
  TOUCHSTART = 'touchstart',
  TOUCHMOVE = 'touchmove',
  TOUCHEND = 'touchend',
  MOUSEDOWN = 'mousedown',
  MOUSEMOVE = 'mousemove',
  MOUSEUP = 'mouseup',
}

export enum EVisualPosition {
  TOP = 'top',
  BOTTOM = 'bottom',
}
